import {NextResponse} from "next/server";import {prisma} from "@/lib/prisma";import {encryptToken} from "@/lib/crypto";import {consumeState} from "@/lib/oauth";
export async function GET(req){const u=new URL(req.url),code=u.searchParams.get("code"),state=u.searchParams.get("state");if(!(await consumeState("youtube",state)))return NextResponse.json({error:"OAuth state tidak valid/kedaluwarsa"},{status:400});if(!code)return NextResponse.json({error:"OAuth code tidak ditemukan"},{status:400});const body=new URLSearchParams({code,client_id:process.env.YOUTUBE_CLIENT_ID,client_secret:process.env.YOUTUBE_CLIENT_SECRET,redirect_uri:`${process.env.APP_URL}/api/auth/youtube/callback`,grant_type:"authorization_code"});const r=await fetch("https://oauth2.googleapis.com/token",{method:"POST",headers:{"Content-Type":"application/x-www-form-urlencoded"},body});const t=await r.json();if(!r.ok)return NextResponse.json(t,{status:400});await prisma.socialAccount.create({data:{platform:"youtube",accessToken:encryptToken(t.access_token),refreshToken:t.refresh_token?encryptToken(t.refresh_token):null,expiresAt:new Date(Date.now()+t.expires_in*1000),displayName:"YouTube account"}});return NextResponse.redirect(new URL("/?connected=youtube",u));}